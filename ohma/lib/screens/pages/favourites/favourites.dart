import 'package:flutter/material.dart';

class favourites extends StatelessWidget {
  const favourites({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Text("fav"));
  }
}
